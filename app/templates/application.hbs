{{#if session.isAuthenticated}}
  <div class='header'>
    <span class='headline'>
      {{#link-to 'index'}}Create-Your-Own Choose-Your-Own-Adventure{{/link-to}} <span class='small'>BETA</span>
    </span>
  </div>
{{/if}}
{{outlet}}

